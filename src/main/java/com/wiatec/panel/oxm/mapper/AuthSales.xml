<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiatec.panel.oxm.dao.AuthSalesDao">
    <resultMap type="com.wiatec.panel.oxm.pojo.AuthSalesInfo" id="authSalesInfo">
        <id column="saleId" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="firstName" property="firstName"/>
        <result column="lastName" property="lastName"/>
        <result column="ssn" property="ssn"/>
        <result column="email" property="email"/>
        <result column="phone" property="phone"/>
        <result column="createTime" property="createTime"/>
        <collection property="authRentUserInfoList" ofType="com.wiatec.panel.oxm.pojo.AuthRentUserInfo">
            <id column="userId" property="id"/>
            <result column="salesId" property="salesId"/>
            <result column="clientKey" property="clientKey"/>
            <result column="category" property="category"/>
            <result column="mac" property="mac"/>
            <result column="firstName" property="firstName"/>
            <result column="lastName" property="lastName"/>
            <result column="email" property="email"/>
            <result column="phone" property="phone"/>
            <result column="activateTime" property="activateTime"/>
            <result column="status" property="status"/>
            <result column="country" property="country"/>
            <result column="region" property="region"/>
            <result column="city" property="city"/>
            <result column="timeZone" property="timeZone"/>
            <result column="lastOnLineTime" property="lastOnLineTime"/>
        </collection>
    </resultMap>

    <sql id="select">SELECT id saleId, username, password, firstName, lastName, ssn, email, phone, createTime FROM auth_sales</sql>

    <select id="countOne" resultType="int" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        SELECT COUNT(*) FROM auth_sales WHERE username=#{username} AND password=#{password}
    </select>

    <select id="countUsername" resultType="int" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        SELECT COUNT(*) FROM auth_sales WHERE username=#{username}
    </select>

    <select id="countSSN" resultType="int" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        SELECT COUNT(*) FROM auth_sales WHERE ssn=#{ssn}
    </select>

    <select id="countEmail" resultType="int" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        SELECT COUNT(*) FROM auth_sales WHERE email=#{email}
    </select>

    <insert id="insertOne" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        INSERT INTO auth_sales (username, password, firstName, lastName, ssn, email, phone)
        VALUES (#{username}, #{password}, #{firstName}, #{lastName}, #{ssn}, #{email}, #{phone})
    </insert>

    <update id="updatePassword" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        UPDATE auth_sales
        SET password=#{password}
        WHERE id=#{id}
    </update>

    <select id="selectOne" resultMap="authSalesInfo" parameterType="com.wiatec.panel.oxm.pojo.AuthSalesInfo">
        <include refid="select"/>
        WHERE username=#{username}
    </select>

    <select id="selectAll" resultMap="authSalesInfo">
        <include refid="select"/>
    </select>

    <select id="selectAllWithUsers" resultMap="authSalesInfo">
        SELECT auth_sales.id saleId, auth_sales.username, auth_sales.password, auth_sales.firstName,
        auth_sales.lastName, auth_sales.ssn, auth_sales.email, auth_sales.phone, auth_sales.createTime,
        auth_rent_user.id userId, auth_rent_user.salesId, auth_rent_user.clientKey, auth_rent_user.category,
        auth_rent_user.mac, auth_rent_user.firstName, auth_rent_user.lastName, auth_rent_user.email, auth_rent_user.phone,
        auth_rent_user.activateTime, auth_rent_user.status, auth_rent_user.country, auth_rent_user.region, auth_rent_user.city,
        auth_rent_user.timeZone, auth_rent_user.lastOnLineTime
        FROM auth_sales, auth_rent_user
        WHERE auth_sales.id > 0
        AND auth_sales.id = auth_rent_user.salesId
    </select>



</mapper>