<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wiatec.panel.oxm.dao.LogPcpCashActivateDao">

    <insert id="insertOne">
        INSERT IGNORE INTO
            log_pcp_cash_activate (mac, executor, create_time)
        VALUES(#{mac}, #{executor}, now())
    </insert>

</mapper>